#include <stdbool.h>
#include <stdlib.h>

bool fpcmp(float x, float y)
{
  return x < y;
}

float fpadd(float x, float y)
{
  return x + y;
}

float fpsub(float x, float y)
{
  return x - y;
}

float fpmul(float x, float y)
{
  return x * y;
}

float fpdiv(float x, float y)
{
  return x / y;
}
