#include "globals.h"

  extern TYPE a[LEN];
int s452()
{

//	intrinsic functions
//	seq function

		for (int i = 0; i < LEN; i++) {
			a[i] = b[i] + c[i] * (TYPE) (val);
			a[i] = a[i] + c[i];
		}
  print_local1(a);
	return 0;
}

