#include "globals.h"

int s452()
{

//	intrinsic functions
//	seq function
  TYPE local_a[LEN];

		for (int i = 0; i < LEN; i++) {
			local_a[i] = b[i] + c[i] * (TYPE) (val);
			local_a[i] = local_a[i] + c[i];
		}
  print_local1(local_a);
	return 0;
}

