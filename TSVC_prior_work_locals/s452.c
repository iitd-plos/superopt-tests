#include "globals.h"

int s452()
{

//	intrinsic functions
//	seq function
  TYPE local_a[LEN], local_b[LEN];
  TYPE local_c[LEN];
  init_local2(local_b, local_c);

		for (int i = 0; i < LEN; i++) {
			local_a[i] = local_b[i] + local_c[i] * (TYPE) (val);
			local_a[i] = local_a[i] + local_c[i];
		}
  print_local1(local_a);
	return 0;
}

