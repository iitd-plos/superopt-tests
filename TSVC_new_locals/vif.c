#include "globals.h"

int vif()
{

//	control loops
//	vector if

  TYPE local_a[LEN];
		for (int i = 0; i < LEN; i++) {
			if (b[i] > (TYPE)0.) {
				local_a[i] = b[i];
			}
		}
    print_local1(local_a);
	return 0;
}
